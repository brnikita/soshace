'use strict';

/**
 * Расширение для Backbone.Validation
 */

define([
    'jquery',
    'underscore',
    'backbone',
    'backbone.layoutmanager',
    'backbone.validation'
], function ($, _, Backbone) {
    _.extend(Backbone.Validation.validators, {
        //пока пусто(
    });
});