'use strict';
(function (Soshace) {
    /**
     * Класс истории, используется для работы с History API
     * или с location, если API не поддерживается
     *
     * @module Soshace.core.History
     */
    Soshace.core.History = Soshace.core.Class.extend({

    });
})(window.Soshace);